<?xml version="1.0" encoding="utf-8"?>
<!-- This project has zero external package dependencies (System.Text.Json is built
     into .NET 8), so package sources are cleared to allow fully offline builds.
     Remove or edit this file if you later add NuGet packages. -->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
